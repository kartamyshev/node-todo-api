<!DOCTYPE html>
html(lang="en")
  include partials/head
  body
    include partials/header
    include partials/navigation
    unless todos.length
      p No todos
    else
      ul.headers
        each todo in todos
          li.todo
            strong= todo.text
